1. start cli
2. import file
3. create pd.DataFrame
4. store words on each line as an entry in the first column
5. check if word is available in given API
6. if available, store a definition, example sentence, and hanja
7. mark if any of the above properties available and notify user
8. else if the word is not available, prompt user to input it themselves

code in python
it'd be a cli app originally

ie. # bash
    ` flaschards <import_file> <output_file> `

take in list of words
find translation of those words
make flashcards